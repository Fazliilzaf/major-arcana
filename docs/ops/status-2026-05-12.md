# Major Arcana — nuläge och nästa leveranser (A)

Datum: 2026-05-12  
Syfte: En gemensam sanningskälla för planering. Uppdatera vid milstolpar eller minst månadsvis.  
Arbetsfördelning: **CCO-ytan** (preview, UX, större UI-experiment) byggs i **Cloud AI**; **detta repo + Arcana-backend, gateway, drift, strategidokument** fortsätter här (Cursor) så länge inget annat överenskommits.

---

## 1. Snabbverifiering (kör om innan viktiga beslut)

Kör i katalogen `major-arcana/`:

```bash
npm run check:syntax
npm run lint:no-bypass
npm test
```

Valfritt (tyngre, närmare “go-live”):

```bash
npm run report:closure-status
# Med Arcana igång + owner-credentials (se runbooks):
npm run report:stability-window
# eller strict:
npm run report:stability-window:strict
# Lokal kedja (startar server på PORT, kör strict suite):
bash ./scripts/run-ops-suite-local.sh
```

### Resultat 2026-05-12 (iCloud-klonen)

| Steg | Utfall |
|------|--------|
| `npm run check:syntax` | OK |
| `npm run lint:no-bypass` | OK |
| `npm test` | OK (869 tester, 2026-05-12 efter test-/copy-justeringar) |
| `npm run report:closure-status` | OK (2026-05-12T21:50Z) — se **§8** |

Senaste **fulla** checklist-evidens innan detta datum: `docs/ops/master-checklist-status-2026-02-26.md` och `docs/ops/final-readiness-sweep-2026-02-26.md` (antag inte att feb-läget gäller utan att återköra).

---

## 2. Arcana backend / plattform (spår Cursor)

**Pilot 1** (admin core, mallar, risk, audit, backup, mail seeds) är levererad enligt `docs/Pilot-1-slutrapport.md`.

**P0 ExecutionGateway** och tillhörande gates, no-bypass-lint och cross-tenant-tester behandlas som **klara** enligt `docs/ops/p0-reality-audit-rerun-2026-02-25.md` och kod under `src/gateway/`.

**Master-checklist P0–P4** (2026-02-26) markerar i stort sett allt som `Done` utom:

- Extern **pentest** (ersätt placeholder i `docs/security/pentest-latest.md`).
- **14–30 dagars** kontinuerlig drift utan no-go-triggers (var `In progress` i feb — måste **tidstämplas om** när ni kör igen).
- **Formell live sign-off** (Owner + Risk + Ops).

Strategisk fullbild (workstreams, agenter, patientkanal, gap): `docs/strategy/arcana-master-plan-punktvis.md` — använd som **north star**; detaljerad fas-2-lista utan bockar: `docs/strategy/arcana-phase-2-masterplan.md`.

---

## 3. CCO / `major-arcana-preview` (spår Cloud AI + repo-filer)

Operativ låsning och målbild: `docs/cco-ny-project-lock.md`, mail-foundation: `docs/cco-mail-foundation-status.md`.

I **repo-roten** (iCloud): `README-CCO.md` (2026-05-07/08) anger:

- **Fas 1–3** (cleanup, observers, design tokens / layers): **gjorda** i denna klon.
- **Fas 4** (state-konsolidering i `app.js`): **ej startad** — största tekniska skulden på preview-sidan.

**Viktigt:** Ändringar som görs i Cloud AI måste **återföras eller synkas** till denna klon om GitHub/Render/CI ska spegla verkligheten — annars divergerar test, deploy och dokumentation.

---

## 4. Kartläggning mot masterplanen (förenklad)

| Område | Bedömning maj 2026 |
|--------|---------------------|
| Intern adminplattform + mall/risk/audit | Levererad (Pilot 1); gateway-lås på plats |
| Säkerhet / drift / release-governance (kod) | Mycket byggt per feb-checklista — **återverifiera** |
| Formell go-live (pentest, stabilitet, signoff) | **Ej klar** |
| CCO preview — polish & legacy | Fas 1–3 klara; Fas 4 kvar |
| “Agenter” (COO/CAO/… som produktytor) | Masterplanens **ordning** gäller; separera **infra** från **faktisk agent-UX** när ni prioriterar |
| Patientkanal | Gated; följ Go/No-Go i masterplan + runbooks |

---

## 5. Nästa tre leveranser (uppdaterat 2026-05-12)

1. ~~**Grön testsvit i denna klon**~~ — **Klart** (`npm test` 869 gröna).

2. **Återkör driftkedjan mot målmiljö** — **Delvis:** `report:closure-status` körd lokalt (artefakter: `data/reports/closure-status-latest.{json,md}`). Kör på er Mac eller mot **prod/staging** när ni har credentials:
   - `bash ./scripts/run-ops-suite-local.sh` (eller `BASE_URL=… npm run ops:suite:strict`)
   - `npm run report:stability-window` / `:strict` (kräver **owner-credentials** enligt script)
   - Uppdatera sedan denna statusfil med datum + ev. nya JSON-paths.

3. **Integrationskontrakt CCO ↔ Arcana (utkast)**  
   Lista stabila endpoints och payloads som Cloud AI-CCO får lita på; notera vad som **inte** får ändras utan synk.

---

## 6. Känt fel efter verifiering 2026-05-12

**Uppdatering samma dag:** Följande har åtgärdats — `npm test` är **grön** (869 tester).

Tidigare problem (nu lösta):

1. **`majorArcanaTopbarAnchoring.test.js`** — Bytte strikt ordnings-regex mot **invarianter** i `collapsed-list` (Skickade, Historik, `data-quick-action="studio"`, Studio/Svarstudio-etikett, m.m.) enligt `cco-ny-project-lock.md`.

2. **`majorArcanaRuntimeQueueRender.test.js`** — Truth-worklist: accepterar **Sanningsstyrd** + uppdaterad chip-copy i `index.html`; mailbox-menyn: meta-rad borttagen i renderer → assert på **`label.title`** när capability finns, annars ingen `mailbox-option-meta`.

3. **`majorArcanaOperatorLanguage.test.js`** — `FORBIDDEN_VISIBLE_TERMS` synkad med **`docs/uiux/cco-operator-language.md`** (borttaget: Truth-driven, readout-strängar som inte står i ordlistan).

4. **`index.html`** — Synlig copy justerad mot ordlistan (t.ex. Sanningsvy, Sanningsstyrd, Mejlurval, Öppna Svarstudio, Fokuspass, m.m.).

5. **`majorArcanaPreviewAuthRobustness.test.js`** — Fetch-mock returnerar **`headers.get`** så `apiRequest` kan läsa `content-type`.

6. **`majorArcanaFocusTruthPrimary.test.js`** + **`scripts/diagnostics/capture-live-thread-samples.js`** — Stabil källa i repot (tidigare `.tmp/...` saknades i CI).

7. **`majorArcanaMailboxAdminSignaturePass.test.js`** — Statisk dropdown tillåter **`Live`** eller **`Aktiv`**; **Aktiva mejlkonton** i mailbox-admin footnotes.

---

## 7. GO-körning — closure / drift (2026-05-12T21:50Z UTC)

Kört i `major-arcana/`:

| Kommando | Utfall |
|----------|--------|
| `npm run report:closure-status` | **OK** — skrev `data/reports/closure-status-latest.json` och `.md` |

**Kunde inte köras här (saknar runtime / secrets):**

| Kommando | Utfall |
|----------|--------|
| `bash ./scripts/run-ops-suite-local.sh` | Servern nådde inte `readyz` inom timeout i denna agentmiljö — **kör lokalt på Mac** (repo utanför långsam nätverksdisk om möjligt). |
| `node ./scripts/report-stability-window.js` | Avbruten: **`Owner credentials saknas`** — sätt enligt `docs/ops/arcana-finalization-runbook.md` (t.ex. `ARCANA_OWNER_*` / `BASE_URL`). |

### Closure-sammanfattning (från `closure-status-latest.json`)

- **done:** nej  
- **progress:** 1/3 förväntade steg (~33 %) — `external_pentest_evidence` räknas som **klar** i denna körning; kvar i `remainingExpectedIds`: **`stability_window_14_30d`**, **`formal_live_signoff`**  
- **pentestBlocked:** nej  
- **stabilityBlocked:** ja  
- **formell sign-off:** väntar (beror på stabilitetsfönster)  
- **Källor som saknades lokalt:** `finalization-sweep-latest.json`, `release-readiness-latest.json`, `stability-window-latest.json` (exists=no) — normalt när ni inte kört sweeps mot samma miljö nyligen.

**Nästa manuella steg för er:** mot **staging/prod** med giltig `BASE_URL` + owner-env: kör `npm run pilot:public` eller full finalization-kedja enligt `docs/ops/arcana-finalization-runbook.md`, sedan `npm run report:closure-status` igen och ev. kopiera in nya artefakter till `data/reports/`.

---

## 8. Relaterade dokument (för djupdykning)

| Fil | Roll |
|-----|------|
| `docs/strategy/arcana-master-plan-punktvis.md` | Helhetsplan |
| `docs/ops/master-checklist-status-2026-02-26.md` | Senaste detaljerade P0–P4-bockar |
| `docs/Pilot-1-slutrapport.md` | Pilotbaseline |
| `docs/cco-ny-project-lock.md` | CCO-låsta beslut |
| `README-CCO.md` (repo root) | CCO fas 1–4 i denna klon |

Nästa statusfil kan heta `docs/ops/status-YYYY-MM-DD.md` och bara peka hit för historik om ni vill undvika att redigera denna retroaktivt.
